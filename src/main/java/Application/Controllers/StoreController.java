/*package main.java.Application.Controllers;

import entities.Seed;
import entities.Shop;
import javafx.application.Application;
import javafx.scene.Group;
import javafx.scene.layout.TilePane;
import javafx.stage.Stage;

import javax.swing.text.html.ImageView;
import java.awt.*;
import java.util.Arrays;
import java.util.List;


public class StoreController extends Application {

    private Shop shop;

   @Override
    public void start(Stage primaryStage){
        List<Seed> seeds = shop.getSeeds();
    }

    TilePane shopPane = new TilePane();
    //TODO: Sätta gaps mellan containers.

        for(Seed seed : seeds){
            Group group = new Group();

            ImageView itemImage = new ImageView(new Image(seed.getImageFilePath()));
            itemImage.setFill()
        }

    private void setupMouseClick(Group group) {

        group.setOnMouseClicked(event -> {

        });
    }
}*/
